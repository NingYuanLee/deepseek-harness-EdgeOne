export const sandboxBrowserMarkup = [
  '<style>',
  '#dsh-makers-actions button{display:inline-flex;align-items:center;gap:7px;height:34px;padding:0 12px;border:none;border-radius:9px;color:#57606a;background:transparent;font:inherit;font-size:14px;font-weight:500;line-height:20px;white-space:nowrap;cursor:pointer;transition:background .16s,color .16s}',
  '#dsh-makers-actions button:hover{background:#f2f4f7;color:#1f2328}',
  '#dsh-makers-actions button:focus-visible{outline:2px solid var(--dsw-alias-brand-primary,#3964fe);outline-offset:1px}',
  '@container dsh-center (max-width:880px){#dsh-makers-actions .dsh-makers-action-label{display:none}#dsh-makers-actions button{padding:0 8px}}',
  '#dsh-makers-sandbox{position:fixed;inset:0;z-index:2147483000;display:flex;justify-content:flex-end;background:rgba(17,24,39,.28)}',
  '#dsh-makers-sandbox[hidden]{display:none!important}',
  '#dsh-makers-sandbox-panel{position:relative;display:flex;flex-direction:column;width:min(420px,100vw);height:100%;background:#fff;box-shadow:-12px 0 32px rgba(15,23,42,.12)}',
  '#dsh-makers-sandbox-head{display:flex;align-items:center;gap:8px;padding:16px 16px 12px;border-bottom:1px solid #e7e9ee}',
  '#dsh-makers-sandbox-head h2{margin:0;flex:1;font-size:15px;font-weight:600;line-height:22px;color:#1f2328}',
  '#dsh-makers-sandbox-head button{width:28px;height:28px;padding:0;border:1px solid #d0d7de;border-radius:7px;background:#fff;color:#57606a;display:inline-flex;align-items:center;justify-content:center;cursor:pointer}',
  '#dsh-makers-sandbox-head button svg{display:block}',
  '#dsh-makers-sandbox-list{flex:1;margin:0;padding:8px 0;overflow:auto}',
  '#dsh-makers-sandbox-list li{list-style:none;display:flex;align-items:center;gap:8px;padding:7px 16px;font-size:13px;line-height:20px;color:#1f2328}',
  '#dsh-makers-sandbox-list li[data-type=directory]{color:#57606a;font-weight:600}',
  '#dsh-makers-sandbox-empty,#dsh-makers-sandbox-error{margin:24px 16px;color:#8b949e;font-size:13px;line-height:20px}',
  '#dsh-makers-sandbox-list .dsh-makers-sandbox-actions{margin-left:auto;display:flex;gap:4px;flex:none}',
  '#dsh-makers-sandbox-list button{width:26px;height:26px;padding:0;border:1px solid #d0d7de;border-radius:7px;background:#fff;color:#3964fe;display:inline-flex;align-items:center;justify-content:center;font:inherit;cursor:pointer}',
  '#dsh-makers-sandbox-list button svg{display:block}',
  '#dsh-makers-sandbox-list button.dsh-makers-sandbox-mention{font-size:15px;font-weight:600;line-height:1;color:#3964fe}',
  '#dsh-makers-sandbox-list button.dsh-makers-sandbox-delete{color:#cf222e;border-color:#ffc1c7}',
  '#dsh-makers-sandbox-list button:disabled,#dsh-makers-sandbox-head button:disabled{opacity:.5;cursor:default}',
  '#dsh-makers-sandbox-confirm{position:absolute;inset:0;z-index:2;display:flex;align-items:flex-end;background:rgba(255,255,255,.72)}',
  '#dsh-makers-sandbox-confirm[hidden]{display:none!important}',
  '#dsh-makers-sandbox-confirm-card{margin:16px;width:calc(100% - 32px);padding:14px 16px;border:1px solid #e7e9ee;border-radius:12px;background:#fff;box-shadow:0 8px 24px rgba(15,23,42,.12)}',
  '#dsh-makers-sandbox-confirm-text{margin:0 0 12px;font-size:13px;line-height:20px;color:#1f2328}',
  '#dsh-makers-sandbox-confirm-actions{display:flex;justify-content:flex-end;gap:8px}',
  '#dsh-makers-sandbox-confirm-actions button{height:32px;padding:0 12px;border:1px solid #d0d7de;border-radius:8px;background:#fff;color:#57606a;font:inherit;font-size:13px;cursor:pointer}',
  '#dsh-makers-sandbox-confirm-actions [data-act=confirm-ok]{background:#cf222e;border-color:#cf222e;color:#fff}',
  '</style><script>',
  '(() => {',
  '  const copy = {',
  '    zh: { files: "沙箱文件", title: "EdgeOne 沙箱", refresh: "刷新", close: "关闭", upload: "上传", delete: "删除", confirmFile: "确定删除文件「{name}」？删除后无法恢复。", confirmDir: "确定删除文件夹「{name}」及其全部内容？删除后无法恢复。", confirmCancel: "取消", confirmOk: "删除", empty: "还没有文件。可以上传，或让智能体写入后出现在这里。", download: "下载", mention: "引用", error: "无法读取沙箱文件" },',
  '    en: { files: "Sandbox files", title: "EdgeOne Sandbox", refresh: "Refresh", close: "Close", upload: "Upload", delete: "Delete", confirmFile: "Delete file \\"{name}\\"? This cannot be undone.", confirmDir: "Delete folder \\"{name}\\" and everything inside? This cannot be undone.", confirmCancel: "Cancel", confirmOk: "Delete", empty: "No files yet. Upload one, or they appear here after the agent writes to the sandbox.", download: "Download", mention: "@", error: "Could not read sandbox files" }',
  '  };',
  '  const localeOf = () => ((document.documentElement.lang || "").toLowerCase().startsWith("en") ? "en" : "zh");',
  '  const t = () => copy[localeOf()];',
  '  const filesIcon = \'<svg viewBox="0 0 16 16" width="18" height="18" aria-hidden="true"><path fill="currentColor" d="M1.75 2A1.75 1.75 0 0 0 0 3.75v8.5C0 13.216.784 14 1.75 14h12.5A1.75 1.75 0 0 0 16 12.25v-6.5A1.75 1.75 0 0 0 14.25 4H7.72l-.97-1.22A1.75 1.75 0 0 0 5.4 2H1.75Z"/></svg>\';',
  '  const uploadIcon = \'<svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true"><path d="M3.5 9.75v2.75h9V9.75" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/><path d="M8 11.25V3.25M5.25 5.75 8 3.25l2.75 2.5" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>\';',
  '  const refreshIcon = \'<svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true"><path d="M13 8a5 5 0 1 1-1.5-3.5" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/><path d="M13 3.25v3.25h-3.25" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>\';',
  '  const closeIcon = \'<svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true"><path d="M4 4l8 8M12 4l-8 8" fill="none" stroke="currentColor" stroke-width="1.55" stroke-linecap="round"/></svg>\';',
  '  const downloadIcon = \'<svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true"><path d="M3.5 9.75v2.75h9V9.75" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/><path d="M8 3.25v8M5.25 8.75 8 11.25l2.75-2.5" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>\';',
  '  const trashIcon = \'<svg viewBox="0 0 16 16" width="14" height="14" aria-hidden="true"><path d="M3.25 4.5h9.5M6.25 4.5V3.25h3.5V4.5M5.25 6.25v6.5h5.5v-6.5" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>\';',
  '  let pendingDelete = null;',
  '  const labelButton = (el, value) => {',
  '    if (!el) return;',
  '    el.title = value;',
  '    el.setAttribute("aria-label", value);',
  '  };',
  '  const iconButton = (icon, label, className) => {',
  '    const button = document.createElement("button");',
  '    button.type = "button";',
  '    if (className) button.className = className;',
  '    labelButton(button, label);',
  '    button.innerHTML = icon;',
  '    return button;',
  '  };',
  '  const hideConfirm = () => {',
  '    pendingDelete = null;',
  '    const confirm = document.getElementById("dsh-makers-sandbox-confirm");',
  '    if (confirm) confirm.hidden = true;',
  '  };',
  '  const panel = () => {',
  '    let root = document.getElementById("dsh-makers-sandbox");',
  '    if (root) return root;',
  '    root = document.createElement("div");',
  '    root.id = "dsh-makers-sandbox";',
  '    root.hidden = true;',
  '    root.innerHTML = \'<div id="dsh-makers-sandbox-panel" role="dialog" aria-modal="true" aria-labelledby="dsh-makers-sandbox-title"><div id="dsh-makers-sandbox-head"><h2 id="dsh-makers-sandbox-title"></h2><button type="button" data-act="upload"></button><button type="button" data-act="refresh"></button><button type="button" data-act="close"></button></div><input id="dsh-makers-sandbox-upload" type="file" multiple hidden><p id="dsh-makers-sandbox-empty" hidden></p><p id="dsh-makers-sandbox-error" hidden></p><ul id="dsh-makers-sandbox-list"></ul><div id="dsh-makers-sandbox-confirm" hidden><div id="dsh-makers-sandbox-confirm-card"><p id="dsh-makers-sandbox-confirm-text"></p><div id="dsh-makers-sandbox-confirm-actions"><button type="button" data-act="confirm-cancel"></button><button type="button" data-act="confirm-ok"></button></div></div></div></div>\';',
  '    root.querySelector("[data-act=upload]").innerHTML = uploadIcon;',
  '    root.querySelector("[data-act=refresh]").innerHTML = refreshIcon;',
  '    root.querySelector("[data-act=close]").innerHTML = closeIcon;',
  '    root.addEventListener("click", (event) => {',
  '      if (event.target === root) close();',
  '    });',
  '    root.querySelector("[data-act=close]").addEventListener("click", close);',
  '    root.querySelector("[data-act=refresh]").addEventListener("click", () => load());',
  '    root.querySelector("[data-act=upload]").addEventListener("click", () => {',
  '      root.querySelector("#dsh-makers-sandbox-upload").click();',
  '    });',
  '    root.querySelector("#dsh-makers-sandbox-upload").addEventListener("change", (event) => {',
  '      const input = event.target;',
  '      uploadFiles(input.files).finally(() => { input.value = ""; });',
  '    });',
  '    root.querySelector("[data-act=confirm-cancel]").addEventListener("click", hideConfirm);',
  '    root.querySelector("[data-act=confirm-ok]").addEventListener("click", () => confirmDelete());',
  '    const board = root.querySelector("#dsh-makers-sandbox-panel");',
  '    board.addEventListener("dragover", (event) => { event.preventDefault(); });',
  '    board.addEventListener("drop", (event) => {',
  '      event.preventDefault();',
  '      uploadFiles(event.dataTransfer && event.dataTransfer.files);',
  '    });',
  '    document.body.append(root);',
  '    return root;',
  '  };',
  '  const applyCopy = () => {',
  '    const text = t();',
  '    const root = document.getElementById("dsh-makers-sandbox");',
  '    if (!root) return;',
  '    root.querySelector("#dsh-makers-sandbox-title").textContent = text.title;',
  '    labelButton(root.querySelector("[data-act=refresh]"), text.refresh);',
  '    labelButton(root.querySelector("[data-act=close]"), text.close);',
  '    labelButton(root.querySelector("[data-act=upload]"), text.upload);',
  '    root.querySelector("[data-act=confirm-cancel]").textContent = text.confirmCancel;',
  '    root.querySelector("[data-act=confirm-ok]").textContent = text.confirmOk;',
  '    root.querySelector("#dsh-makers-sandbox-empty").textContent = text.empty;',
  '    const files = document.getElementById("dsh-makers-sandbox-files");',
  '    if (files) {',
  '      files.title = text.files;',
  '      files.querySelector(".dsh-makers-action-label").textContent = text.files;',
  '    }',
  '  };',
  '  const close = () => {',
  '    hideConfirm();',
  '    const root = document.getElementById("dsh-makers-sandbox");',
  '    if (root) root.hidden = true;',
  '  };',
  '  const mentionOf = (path) => (/\\s/.test(path) ? \'@"\' + path + \'"\' : "@" + path);',
  '  const focusComposer = () => {',
  '    const editor = document.querySelector("[data-lexical-editor=true]");',
  '    if (editor instanceof HTMLElement) editor.focus();',
  '  };',
  '  const insertPlainMention = (text) => {',
  '    const editor = document.querySelector("[data-lexical-editor=true]");',
  '    if (!(editor instanceof HTMLElement)) return;',
  '    editor.focus();',
  '    document.execCommand("insertText", false, text + " ");',
  '  };',
  '  const mention = (item) => {',
  '    const path = item.path || item.name || "";',
  '    if (!path) return;',
  '    const value = mentionOf(path);',
  '    const event = new CustomEvent("dsh-makers:insert-reference", {',
  '      cancelable: true,',
  '      detail: { path, label: item.name || path.split("/").pop() || path, mention: value },',
  '    });',
  '    window.dispatchEvent(event);',
  '    if (!event.defaultPrevented) insertPlainMention(value);',
  '    close();',
  '    focusComposer();',
  '  };',
  '  const showError = (reason) => {',
  '    const error = document.getElementById("dsh-makers-sandbox-error");',
  '    if (!error) return;',
  '    error.hidden = false;',
  '    error.textContent = reason instanceof Error ? reason.message : t().error;',
  '  };',
  '  const bytesToBase64 = (bytes) => {',
  '    let binary = "";',
  '    const chunk = 0x8000;',
  '    for (let i = 0; i < bytes.length; i += chunk) binary += String.fromCharCode.apply(null, Array.from(bytes.subarray(i, i + chunk)));',
  '    return btoa(binary);',
  '  };',
  '  const uploadFiles = async (files) => {',
  '    if (!files || files.length === 0) return;',
  '    hideConfirm();',
  '    const upload = document.querySelector("[data-act=upload]");',
  '    if (upload) upload.disabled = true;',
  '    try {',
  '      for (const file of files) {',
  '        const bytes = new Uint8Array(await file.arrayBuffer());',
  '        const response = await fetch("/api/sandbox/file", {',
  '          method: "POST",',
  '          headers: { "content-type": "application/json" },',
  '          body: JSON.stringify({ path: file.name, contentBase64: bytesToBase64(bytes) }),',
  '        });',
  '        const body = await response.json().catch(() => ({}));',
  '        if (!response.ok) throw new Error(body.error || t().error);',
  '      }',
  '      await load();',
  '    } catch (reason) {',
  '      showError(reason);',
  '    } finally {',
  '      if (upload) upload.disabled = false;',
  '    }',
  '  };',
  '  const askDelete = (item) => {',
  '    const path = item.path || "";',
  '    if (!path) return;',
  '    pendingDelete = item;',
  '    const text = t();',
  '    const name = item.name || path.split("/").pop() || path;',
  '    const template = item.type === "directory" ? text.confirmDir : text.confirmFile;',
  '    const root = panel();',
  '    root.querySelector("#dsh-makers-sandbox-confirm-text").textContent = template.replace("{name}", name);',
  '    root.querySelector("[data-act=confirm-cancel]").textContent = text.confirmCancel;',
  '    root.querySelector("[data-act=confirm-ok]").textContent = text.confirmOk;',
  '    root.querySelector("#dsh-makers-sandbox-confirm").hidden = false;',
  '  };',
  '  const confirmDelete = async () => {',
  '    const item = pendingDelete;',
  '    const path = item && item.path;',
  '    if (!path) { hideConfirm(); return; }',
  '    hideConfirm();',
  '    try {',
  '      const response = await fetch("/api/sandbox/file?path=" + encodeURIComponent(path), { method: "DELETE" });',
  '      const body = await response.json().catch(() => ({}));',
  '      if (!response.ok) throw new Error(body.error || t().error);',
  '      await load();',
  '    } catch (reason) {',
  '      showError(reason);',
  '    }',
  '  };',
  '  const download = async (path, name) => {',
  '    const response = await fetch("/api/sandbox/file?path=" + encodeURIComponent(path));',
  '    if (!response.ok) throw new Error(await response.text());',
  '    const blob = await response.blob();',
  '    const url = URL.createObjectURL(blob);',
  '    const link = document.createElement("a");',
  '    link.href = url;',
  '    link.download = name;',
  '    link.click();',
  '    setTimeout(() => URL.revokeObjectURL(url), 30_000);',
  '  };',
  '  const load = async () => {',
  '    const root = panel();',
  '    applyCopy();',
  '    hideConfirm();',
  '    const list = root.querySelector("#dsh-makers-sandbox-list");',
  '    const empty = root.querySelector("#dsh-makers-sandbox-empty");',
  '    const error = root.querySelector("#dsh-makers-sandbox-error");',
  '    list.replaceChildren();',
  '    empty.hidden = true;',
  '    error.hidden = true;',
  '    try {',
  '      const response = await fetch("/api/sandbox/files");',
  '      const body = await response.json();',
  '      if (!response.ok) throw new Error(body.error || t().error);',
  '      const items = Array.isArray(body.items) ? body.items : [];',
  '      if (items.length === 0) {',
  '        empty.hidden = false;',
  '        return;',
  '      }',
  '      for (const item of items) {',
  '        const row = document.createElement("li");',
  '        row.dataset.type = item.type;',
  '        row.style.paddingLeft = (16 + Number(item.depth || 0) * 14) + "px";',
  '        row.append(item.type === "directory" ? "📁 " : "📄 ", item.path || item.name || "");',
  '        const actions = document.createElement("div");',
  '        actions.className = "dsh-makers-sandbox-actions";',
  '        const cite = document.createElement("button");',
  '        cite.type = "button";',
  '        cite.className = "dsh-makers-sandbox-mention";',
  '        cite.textContent = "@";',
  '        labelButton(cite, t().mention);',
  '        cite.addEventListener("click", () => mention(item));',
  '        actions.append(cite);',
  '        if (item.type === "file") {',
  '          const button = iconButton(downloadIcon, t().download);',
  '          button.addEventListener("click", () => download(item.path, item.name || item.path).catch(showError));',
  '          actions.append(button);',
  '        }',
  '        const trash = iconButton(trashIcon, t().delete, "dsh-makers-sandbox-delete");',
  '        trash.addEventListener("click", () => askDelete(item));',
  '        actions.append(trash);',
  '        row.append(actions);',
  '        list.append(row);',
  '      }',
  '    } catch (reason) {',
  '      error.hidden = false;',
  '      error.textContent = reason instanceof Error ? reason.message : t().error;',
  '    }',
  '  };',
  '  const open = () => {',
  '    panel().hidden = false;',
  '    load();',
  '  };',
  '  window.addEventListener("dsh-makers:open-sandbox", open);',
  '  const attachButton = (nav) => {',
  '    if (!nav || document.getElementById("dsh-makers-sandbox-files")) return;',
  '    const button = document.createElement("button");',
  '    button.id = "dsh-makers-sandbox-files";',
  '    button.type = "button";',
  '    button.innerHTML = filesIcon + \'<span class="dsh-makers-action-label"></span>\';',
  '    button.addEventListener("click", open);',
  '    nav.insertBefore(button, nav.firstChild);',
  '    applyCopy();',
  '  };',
  '  const watch = () => {',
  '    const nav = document.getElementById("dsh-makers-actions");',
  '    attachButton(nav);',
  '  };',
  '  watch();',
  '  new MutationObserver(watch).observe(document.documentElement, { childList: true, subtree: true });',
  '  new MutationObserver(applyCopy).observe(document.documentElement, { attributes: true, attributeFilter: ["lang"] });',
  '})();',
  '</script>',
].join('\n')
