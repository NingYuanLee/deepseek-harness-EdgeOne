export const sandboxBrowserMarkup = [
  '<style>',
  '#dsh-makers-actions button{display:inline-flex;align-items:center;gap:7px;height:34px;padding:0 12px;border:none;border-radius:9px;color:#57606a;background:transparent;font:inherit;font-size:14px;font-weight:500;line-height:20px;white-space:nowrap;cursor:pointer;transition:background .16s,color .16s}',
  '#dsh-makers-actions button:hover{background:#f2f4f7;color:#1f2328}',
  '#dsh-makers-actions button:focus-visible{outline:2px solid var(--dsw-alias-brand-primary,#3964fe);outline-offset:1px}',
  '@container dsh-center (max-width:880px){#dsh-makers-actions .dsh-makers-action-label{display:none}#dsh-makers-actions button{padding:0 8px}}',
  '#dsh-makers-sandbox{position:fixed;inset:0;z-index:2147483000;display:flex;justify-content:flex-end;background:rgba(17,24,39,.28)}',
  '#dsh-makers-sandbox[hidden]{display:none!important}',
  '#dsh-makers-sandbox-panel{display:flex;flex-direction:column;width:min(420px,100vw);height:100%;background:#fff;box-shadow:-12px 0 32px rgba(15,23,42,.12)}',
  '#dsh-makers-sandbox-head{display:flex;align-items:center;gap:8px;padding:16px 16px 12px;border-bottom:1px solid #e7e9ee}',
  '#dsh-makers-sandbox-head h2{margin:0;flex:1;font-size:15px;font-weight:600;line-height:22px;color:#1f2328}',
  '#dsh-makers-sandbox-head button{height:28px;padding:0 8px;border:1px solid #d0d7de;border-radius:7px;background:#fff;color:#57606a;font:inherit;font-size:12px;cursor:pointer}',
  '#dsh-makers-sandbox-list{flex:1;margin:0;padding:8px 0;overflow:auto}',
  '#dsh-makers-sandbox-list li{list-style:none;display:flex;align-items:center;gap:8px;padding:7px 16px;font-size:13px;line-height:20px;color:#1f2328}',
  '#dsh-makers-sandbox-list li[data-type=directory]{color:#57606a;font-weight:600}',
  '#dsh-makers-sandbox-empty,#dsh-makers-sandbox-error{margin:24px 16px;color:#8b949e;font-size:13px;line-height:20px}',
  '#dsh-makers-sandbox-list .dsh-makers-sandbox-actions{margin-left:auto;display:flex;gap:6px;flex:none}',
  '#dsh-makers-sandbox-list button{height:26px;padding:0 8px;border:1px solid #d0d7de;border-radius:7px;background:#fff;color:#3964fe;font:inherit;font-size:12px;cursor:pointer}',
  '#dsh-makers-sandbox-list button.dsh-makers-sandbox-delete{color:#cf222e;border-color:#ffc1c7}',
  '#dsh-makers-sandbox-list button:disabled,#dsh-makers-sandbox-head button:disabled{opacity:.5;cursor:default}',
  '</style><script>',
  '(() => {',
  '  const copy = {',
    '    zh: { files: "沙箱文件", title: "EdgeOne 沙箱", refresh: "刷新", close: "关闭", upload: "上传", delete: "删除", confirmFile: "确定删除这个文件？", confirmDir: "确定删除这个文件夹及其内容？", empty: "还没有文件。可以上传，或让智能体写入后出现在这里。", download: "下载", mention: "引用", error: "无法读取沙箱文件" },',
  '    en: { files: "Sandbox files", title: "EdgeOne Sandbox", refresh: "Refresh", close: "Close", upload: "Upload", delete: "Delete", confirmFile: "Delete this file?", confirmDir: "Delete this folder and its contents?", empty: "No files yet. Upload one, or they appear here after the agent writes to the sandbox.", download: "Download", mention: "@", error: "Could not read sandbox files" }',
  '  };',
  '  const localeOf = () => ((document.documentElement.lang || "").toLowerCase().startsWith("en") ? "en" : "zh");',
  '  const t = () => copy[localeOf()];',
  '  const filesIcon = \'<svg viewBox="0 0 16 16" width="18" height="18" aria-hidden="true"><path fill="currentColor" d="M1.75 2A1.75 1.75 0 0 0 0 3.75v8.5C0 13.216.784 14 1.75 14h12.5A1.75 1.75 0 0 0 16 12.25v-6.5A1.75 1.75 0 0 0 14.25 4H7.72l-.97-1.22A1.75 1.75 0 0 0 5.4 2H1.75Z"/></svg>\';',
  '  const panel = () => {',
  '    let root = document.getElementById("dsh-makers-sandbox");',
  '    if (root) return root;',
  '    root = document.createElement("div");',
  '    root.id = "dsh-makers-sandbox";',
  '    root.hidden = true;',
  '    root.innerHTML = \'<div id="dsh-makers-sandbox-panel" role="dialog" aria-modal="true" aria-labelledby="dsh-makers-sandbox-title"><div id="dsh-makers-sandbox-head"><h2 id="dsh-makers-sandbox-title"></h2><button type="button" data-act="upload"></button><button type="button" data-act="refresh"></button><button type="button" data-act="close"></button></div><input id="dsh-makers-sandbox-upload" type="file" multiple hidden><p id="dsh-makers-sandbox-empty" hidden></p><p id="dsh-makers-sandbox-error" hidden></p><ul id="dsh-makers-sandbox-list"></ul></div>\';',
  '    root.addEventListener("click", (event) => {',
  '      if (event.target === root) close();',
  '    });',
  '    root.querySelector("[data-act=close]").addEventListener("click", close);',
  '    root.querySelector("[data-act=refresh]").addEventListener("click", () => load());',
  '    root.querySelector("[data-act=upload]").addEventListener("click", () => {',
  '      root.querySelector("#dsh-makers-sandbox-upload").click();',
  '    });',
  '    root.querySelector("#dsh-makers-sandbox-upload").addEventListener("change", (event) => {',
  '      const input = event.target;',
  '      uploadFiles(input.files).finally(() => { input.value = ""; });',
  '    });',
  '    const board = root.querySelector("#dsh-makers-sandbox-panel");',
  '    board.addEventListener("dragover", (event) => { event.preventDefault(); });',
  '    board.addEventListener("drop", (event) => {',
  '      event.preventDefault();',
  '      uploadFiles(event.dataTransfer && event.dataTransfer.files);',
  '    });',
  '    document.body.append(root);',
  '    return root;',
  '  };',
  '  const applyCopy = () => {',
  '    const text = t();',
  '    const root = document.getElementById("dsh-makers-sandbox");',
  '    if (!root) return;',
  '    root.querySelector("#dsh-makers-sandbox-title").textContent = text.title;',
  '    root.querySelector("[data-act=refresh]").textContent = text.refresh;',
  '    root.querySelector("[data-act=close]").textContent = text.close;',
  '    root.querySelector("[data-act=upload]").textContent = text.upload;',
  '    root.querySelector("#dsh-makers-sandbox-empty").textContent = text.empty;',
  '    const files = document.getElementById("dsh-makers-sandbox-files");',
  '    if (files) {',
  '      files.title = text.files;',
  '      files.querySelector(".dsh-makers-action-label").textContent = text.files;',
  '    }',
  '  };',
  '  const close = () => {',
  '    const root = document.getElementById("dsh-makers-sandbox");',
  '    if (root) root.hidden = true;',
  '  };',
  '  const mentionOf = (path) => (/\\s/.test(path) ? \'@"\' + path + \'"\' : "@" + path);',
  '  const focusComposer = () => {',
  '    const editor = document.querySelector("[data-lexical-editor=true]");',
  '    if (editor instanceof HTMLElement) editor.focus();',
  '  };',
  '  const insertPlainMention = (text) => {',
  '    const editor = document.querySelector("[data-lexical-editor=true]");',
  '    if (!(editor instanceof HTMLElement)) return;',
  '    editor.focus();',
  '    document.execCommand("insertText", false, text + " ");',
  '  };',
  '  const mention = (item) => {',
  '    const path = item.path || item.name || "";',
  '    if (!path) return;',
  '    const value = mentionOf(path);',
  '    const event = new CustomEvent("dsh-makers:insert-reference", {',
  '      cancelable: true,',
  '      detail: { path, label: item.name || path.split("/").pop() || path, mention: value },',
  '    });',
  '    window.dispatchEvent(event);',
  '    if (!event.defaultPrevented) insertPlainMention(value);',
  '    close();',
  '    focusComposer();',
  '  };',
  '  const showError = (reason) => {',
  '    const error = document.getElementById("dsh-makers-sandbox-error");',
  '    if (!error) return;',
  '    error.hidden = false;',
  '    error.textContent = reason instanceof Error ? reason.message : t().error;',
  '  };',
  '  const bytesToBase64 = (bytes) => {',
  '    let binary = "";',
  '    const chunk = 0x8000;',
  '    for (let i = 0; i < bytes.length; i += chunk) binary += String.fromCharCode.apply(null, Array.from(bytes.subarray(i, i + chunk)));',
  '    return btoa(binary);',
  '  };',
  '  const uploadFiles = async (files) => {',
  '    if (!files || files.length === 0) return;',
  '    const upload = document.querySelector("[data-act=upload]");',
  '    if (upload) upload.disabled = true;',
  '    try {',
  '      for (const file of files) {',
  '        const bytes = new Uint8Array(await file.arrayBuffer());',
  '        const response = await fetch("/api/sandbox/file", {',
  '          method: "POST",',
  '          headers: { "content-type": "application/json" },',
  '          body: JSON.stringify({ path: file.name, contentBase64: bytesToBase64(bytes) }),',
  '        });',
  '        const body = await response.json().catch(() => ({}));',
  '        if (!response.ok) throw new Error(body.error || t().error);',
  '      }',
  '      await load();',
  '    } catch (reason) {',
  '      showError(reason);',
  '    } finally {',
  '      if (upload) upload.disabled = false;',
  '    }',
  '  };',
  '  const removeItem = async (item) => {',
  '    const path = item.path || "";',
  '    if (!path) return;',
  '    if (!window.confirm(item.type === "directory" ? t().confirmDir : t().confirmFile)) return;',
  '    try {',
  '      const response = await fetch("/api/sandbox/file?path=" + encodeURIComponent(path), { method: "DELETE" });',
  '      const body = await response.json().catch(() => ({}));',
  '      if (!response.ok) throw new Error(body.error || t().error);',
  '      await load();',
  '    } catch (reason) {',
  '      showError(reason);',
  '    }',
  '  };',
  '  const download = async (path, name) => {',
  '    const response = await fetch("/api/sandbox/file?path=" + encodeURIComponent(path));',
  '    if (!response.ok) throw new Error(await response.text());',
  '    const blob = await response.blob();',
  '    const url = URL.createObjectURL(blob);',
  '    const link = document.createElement("a");',
  '    link.href = url;',
  '    link.download = name;',
  '    link.click();',
  '    setTimeout(() => URL.revokeObjectURL(url), 30_000);',
  '  };',
  '  const load = async () => {',
  '    const root = panel();',
  '    applyCopy();',
  '    const list = root.querySelector("#dsh-makers-sandbox-list");',
  '    const empty = root.querySelector("#dsh-makers-sandbox-empty");',
  '    const error = root.querySelector("#dsh-makers-sandbox-error");',
  '    list.replaceChildren();',
  '    empty.hidden = true;',
  '    error.hidden = true;',
  '    try {',
  '      const response = await fetch("/api/sandbox/files");',
  '      const body = await response.json();',
  '      if (!response.ok) throw new Error(body.error || t().error);',
  '      const items = Array.isArray(body.items) ? body.items : [];',
  '      if (items.length === 0) {',
  '        empty.hidden = false;',
  '        return;',
  '      }',
  '      for (const item of items) {',
  '        const row = document.createElement("li");',
  '        row.dataset.type = item.type;',
  '        row.style.paddingLeft = (16 + Number(item.depth || 0) * 14) + "px";',
  '        row.append(item.type === "directory" ? "📁 " : "📄 ", item.path || item.name || "");',
  '        const actions = document.createElement("div");',
  '        actions.className = "dsh-makers-sandbox-actions";',
  '        const cite = document.createElement("button");',
  '        cite.type = "button";',
  '        cite.textContent = t().mention;',
  '        cite.addEventListener("click", () => mention(item));',
  '        actions.append(cite);',
  '        if (item.type === "file") {',
  '          const button = document.createElement("button");',
  '          button.type = "button";',
  '          button.textContent = t().download;',
  '          button.addEventListener("click", () => download(item.path, item.name || item.path).catch(showError));',
  '          actions.append(button);',
  '        }',
  '        const trash = document.createElement("button");',
  '        trash.type = "button";',
  '        trash.className = "dsh-makers-sandbox-delete";',
  '        trash.textContent = t().delete;',
  '        trash.addEventListener("click", () => removeItem(item));',
  '        actions.append(trash);',
  '        row.append(actions);',
  '        list.append(row);',
  '      }',
  '    } catch (reason) {',
  '      error.hidden = false;',
  '      error.textContent = reason instanceof Error ? reason.message : t().error;',
  '    }',
  '  };',
  '  const open = () => {',
  '    panel().hidden = false;',
  '    load();',
  '  };',
  '  window.addEventListener("dsh-makers:open-sandbox", open);',
  '  const attachButton = (nav) => {',
  '    if (!nav || document.getElementById("dsh-makers-sandbox-files")) return;',
  '    const button = document.createElement("button");',
  '    button.id = "dsh-makers-sandbox-files";',
  '    button.type = "button";',
  '    button.innerHTML = filesIcon + \'<span class="dsh-makers-action-label"></span>\';',
  '    button.addEventListener("click", open);',
  '    nav.insertBefore(button, nav.firstChild);',
  '    applyCopy();',
  '  };',
  '  const watch = () => {',
  '    const nav = document.getElementById("dsh-makers-actions");',
  '    attachButton(nav);',
  '  };',
  '  watch();',
  '  new MutationObserver(watch).observe(document.documentElement, { childList: true, subtree: true });',
  '  new MutationObserver(applyCopy).observe(document.documentElement, { attributes: true, attributeFilter: ["lang"] });',
  '})();',
  '</script>',
].join('\n')
